import React from 'react';
import styled from 'styled-components';
const ship = require('./sampleData/SampleShips');

const Text = styled.div`
  color: white;
`;

const Ship = () => {
  return(
    <Text>Hellow world</Text>
  )
}

export default Ship;